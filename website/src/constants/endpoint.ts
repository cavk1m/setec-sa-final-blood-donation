// ─── Auth / Users ─────────────────────────────────────────────────────────────
const authEndpoint: string = "/api/users/";

// ─── Survey ───────────────────────────────────────────────────────────────────
const surveyEndpoint: string = "/api/survey/";

// ─── Donation ─────────────────────────────────────────────────────────────────
const donationEndpoint: string = "/api/donation/";

// ─── Campaigns ────────────────────────────────────────────────────────────────
const campaignEndpoint: string = "/api/campaigns/";

// ─── Locations ────────────────────────────────────────────────────────────────
const locationEndpoint: string = "/api/locations/";

// ─── Certificates ─────────────────────────────────────────────────────────────
const certificateEndpoint: string = "/api/certificates/";

// ─── Settings ─────────────────────────────────────────────────────────────────
const settingsEndpoint: string = "/api/settings/";

export {
  authEndpoint,
  surveyEndpoint,
  donationEndpoint,
  campaignEndpoint,
  locationEndpoint,
  certificateEndpoint,
  settingsEndpoint,
};
